@extends('layouts.main')

@section('content')
    <h1>Example {{ $item }} <span class="badge bg-secondary">{{ $label }}</span></h1>
    <h2>Example {{ $item }} <span class="badge bg-secondary">{{ $label }}</span></h2>
    <h3>Example {{ $item }} <span class="badge bg-secondary">{{ $label }}</span></h3>
    <h4>Example {{ $item }} <span class="badge bg-secondary">{{ $label }}</span></h4>
    <h5>Example {{ $item }} <span class="badge bg-secondary">{{ $label }}</span></h5>
    <h6>Example {{ $item }} <span class="badge bg-secondary">{{ $label }}</span></h6>
@endsection
